collections:
  research:

defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "blog_post"
  - scope:
      path: ""
    values:
      layout: "default"

plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-seo-tag
